[pytest]
testpaths = integration
markers =
    integration: Tests requiring sandbox bot credentials (deselect with -m "not integration")

# All tests under integration/ that need real credentials use pytest.skip()
# via the conftest fixtures when env vars are missing, so they can coexist
# in a single test run with unit tests that need nothing.
